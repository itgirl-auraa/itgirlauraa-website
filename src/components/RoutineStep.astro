---
interface Props {
  number: number;
  title: string;
  description: string;
  tip: string;
}

const { number, title, description, tip } = Astro.props;
---

<div class="bg-white rounded-lg shadow-sm p-6 relative">
  <div class="absolute -top-4 -left-4 w-10 h-10 bg-espresso text-white rounded-full flex items-center justify-center font-bold">
    {number}
  </div>
  
  <h3 class="font-display text-xl text-espresso mb-2 pl-6">{title}</h3>
  <p class="text-bronze mb-4">{description}</p>
  
  <div class="bg-beige/20 p-4 rounded-md">
    <p class="text-sm italic flex items-start">
      <span class="text-espresso mr-2">💡</span>
      <span>{tip}</span>
    </p>
  </div>
</div> 